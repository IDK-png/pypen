from setuptools import setup, find_packages

setup(
    name='pypenlib',
    version='0.01',
    description='Libary for pintests that using scapy',
    author='mfochka',
    packages=find_packages(),
)